{
  "name": "pm-awa",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/cds-types": "^0.9.0",
    "@cap-js/sqlite": "^1",
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "start": "cds-serve",
    "start:dev": "concurrently \"cds-watch\" \"npm --prefix app/dashboard run start\""
  }
}
